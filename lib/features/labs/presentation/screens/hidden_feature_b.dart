import 'package:flutter/material.dart';

class HiddenFeatureBScreen extends StatelessWidget {
  const HiddenFeatureBScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Hidden Feature B'),
      ),
    );
  }
}
